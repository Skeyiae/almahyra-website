import React from 'react';

const ModelConfigurator = () => {
    return (
        <section className="py-12 px-4 bg-gray-50">
            <div className="max-w-7xl mx-auto">
                <h2 className="text-3xl font-bold text-gray-900 border-b-2 border-gray-200 pb-4">
                    Konfigurasi Produk
                </h2>
                <div className="mt-8">
                    <p className="text-gray-600 italic">Fitur konfigurator model sedang dalam pengembangan...</p>
                </div>
            </div>
        </section>
    );
};

export default ModelConfigurator;
